defmodule FeedbackApiWeb.UsersControllerTest do
  # use FeedbackApiWeb.ConnCase
  # alias FeedbackApi.{Cohort, User}

  # test "Create users", %{conn: conn} do
  #   conn = conn |> put_req_header("content-type", "application/json")
  #   body = File.read!("test/fixtures/users_create.exs")
  #   conn = post(conn, "/api/v1/users", body)
  #   assert json_response(conn, 201) == %{"success" => "Users updated."}
  #   assert User |> Repo.aggregate(:count, :id) == 12
  # end
end
